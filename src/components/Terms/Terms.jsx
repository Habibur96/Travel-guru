import React from "react";

const Terms = ({info}) => {
  console.log(info);
  // const { name, description } = info;
  return (
    <div>
      {/* <h3>{name}</h3>
      <p>{description}</p> */}
    </div>
  );
};

export default Terms;
